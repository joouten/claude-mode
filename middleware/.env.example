# claude-mode middleware — environment configuration
# Copy this file to `.env` (same directory) and fill in your real key.
# Never commit the real `.env` — it is gitignored at the repo root.

ANTHROPIC_API_KEY=sk-ant-paste-your-key-here
